# Production API (Vercel should also set VITE_API_BASE_URL for overrides).
# Default in code matches this URL if the variable is absent at build time.
VITE_API_BASE_URL=https://petlove.b.goit.study/api
